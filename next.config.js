/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // No need for experimental.appDir in Next.js 13+ as it's the default
  // API routes are automatically enabled in the App Router
}

module.exports = nextConfig